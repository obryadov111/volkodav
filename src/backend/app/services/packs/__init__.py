"""Контент-паки платформ: формат, валидация, манифест для агента, реестр.

Пак — YAML-файл с описанием одной платформы (как её распознать и какие проверки
выполнять). Валидируется при загрузке (models.py), превращается в подписанный
JSON-манифест для агента (manifest.py), проверки оцениваются на сервере
(evaluate.py). Подробнее — app/packs/README.md.
"""
