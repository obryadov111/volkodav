# Сводка работ 2026-09-20 — что сделано, влито и выкачено

Единый документ обо всём, что сделано за этот день: что изменено в коде и документах, что влито в `main`, как и с какими проверками выкачено на живую систему, что проверено, а что нет, как откатить и что осталось. Подробности по этапам — в отчётах: [Этап 0 — реализация фундамента паков, подробный отчёт](stage0-report.md), [Этап 1 — пак ubuntu-server, подробный отчёт](stage1-report.md), [Этап 2 — несколько паков на актив, docker, Cisco, Astra, подробный отчёт](stage2-report.md). План — [Дорожная карта агента — 5 этапов](roadmap.md).

## 1 · Коротко

- **Сделано три этапа плана** (0, 1, 2): формат паков платформ, движок проб, подписанные манифесты, оценка на сервере, четыре пака (`ubuntu-server`, `docker`, `cisco-ios`, `astra-linux`), несколько паков на один актив.
- **Всё влито в `main`** репозитория `volkodav` тремя PR (#1, #7, #8); CI на `main` зелёный (тесты бэкенда и агента).
- **Всё выкачено на живую систему**: сделан и проверен бэкап, применены две миграции, задан ключ подписи паков, бэкенд перезапущен. Существующие данные (4 организации, 10 активов, 101 результат проверки) на месте.
- **Тесты:** 206 бэкенда (было 28) и 93 агента (было 0).
- **Главный результат по существу:** старый агент ложно засчитывал нарушения как выполненные (блокировка учёток, вход root по SSH); новые пробы этого не делают, а при нехватке прав на устройстве сообщают «не проверено», а не «всё хорошо».

## 2 · Что сделано по этапам

| Этап | Суть | Итог |
|---|---|---|
| **0 · Фундамент** | Формат пака (YAML), 7 типов проб только на чтение, операторы сравнения, HMAC-подписанные манифесты, реестр версий, `GET /api/agent/manifests`, расширение `POST /api/ingest`, миграция, режим агента `--use-packs`, транспорты `local` и `ssh`, политика безопасности агента (белые списки, запрет чтения секретов) | PR #1, `1b57213` |
| **1 · Пак ubuntu-server** | Перенос 12 из 14 правил старого агента; регрессионный стенд «старый агент ↔ пак»; найдено и исправлено 7 дефектов старой логики; проба `first_of`, опция `follow_include` (раскрытие `Include`); метаданные результатов в записи (интерфейс без изменений фронтенда) | PR #7, `c37289c` |
| **2 · Несколько паков** | Один прогон — несколько паков — один снимок; паки `docker` (baseline), `cisco-ios` (draft), `astra-linux` (inventory); проба `cmd_foreach`; уровень зрелости `draft`; защита от ложных «выполнено» при нехватке прав на устройстве | PR #8, `12fdaf2` |

### Самое важное из найденного

1. **Ложные «выполнено» в старом агенте** (проверено на реальном сервере): блокировка учётной записи считалась включённой при `faillock.conf` из одних комментариев; вход root по SSH считался запрещённым по умолчанию, хотя у OpenSSH умолчание разрешает вход по ключу; `Include`, порядок значений и блок `Match` разбирались неверно.
2. **Опасность при нехватке прав на сетевом устройстве:** Cisco отвечает `% Invalid input…` вместо конфигурации, и все проверки «параметра нет» ложно проходили бы на пустом выводе. Теперь такая проба даёт `error`.
3. **Числовое сравнение прав файлов некорректно** (`604` численно меньше `640`, но шире по правам): добавлен побитовый оператор `mode_within`.
4. **В CI бэкенда падали тесты на `main`** ещё до работ (нет `pythonpath` для pytest): исправлено.

## 3 · Слияние в `main`

| PR | Что | Merge-коммит | CI |
|---|---|---|---|
| [#1](https://github.com/obryadov111/volkodav/pull/1) | Этап 0 (+ исправление CI, + документация) | `1b57213` | ✅ |
| [#7](https://github.com/obryadov111/volkodav/pull/7) | Этап 1 | `c37289c` | ✅ |
| [#8](https://github.com/obryadov111/volkodav/pull/8) | Этап 2 | `12fdaf2` | ✅ |

Слияние обычным merge-коммитом (не squash): сохранены все отдельные коммиты и их хэши, на которые ссылаются документы. Ветки `feature/stage0-pack-foundation`, `feature/stage1-ubuntu-pack`, `feature/stage2-multipack` остаются на GitHub как история (удалять не пришлось).

## 4 · Выкатка на живую систему

Выполнена 2026-09-20 вечером. **Порядок выбран так, чтобы не было простоя и данные были защищены.**

| # | Шаг | Результат |
|---|---|---|
| 1 | **Разведка (только чтение):** скрипт `deploy/restart_backend.sh`, версия миграций живой БД, счётчики данных | БД на миграции `5bbca4cdfbaa`; 4 организации, 10 активов, 101 результат, 4 ключа агентов, 3 пользователя |
| 2 | **Полный дамп БД** (`pg_dump`) и копия `.env` в `~/backups/` (права 700/600) | 270 КБ, 20 таблиц |
| 3 | **Проверка бэкапа восстановлением** во временной БД, сверка счётчиков | Счётчики совпали по всем таблицам — бэкап пригоден для отката |
| 4 | **Тег на работавший образ** `volkodav-backend:pre-packs` (для отката кода) | ✅ |
| 5 | **Сборка нового образа** из `main` (`12fdaf2`), тег `volkodav-backend:packs` | ✅ |
| 6 | **Миграции до замены контейнера** (разовый контейнер в сети БД): `a3f1c9d27b40`, `b7d2e4f18c63` | БД на `b7d2e4f18c63`; счётчики данных **прежние**; 15 новых колонок на месте. Миграции только добавляют `nullable`-колонки — старый код на них не реагирует, поэтому старый контейнер всё это время работал |
| 7 | **`PACK_SIGNING_KEY`**: сгенерирован (64 символа), добавлен в `.env` (права 600, файл не в git); копия для агентов — `~/.config/hardening-agent/pack.key` (права 600) | Значение нигде не выводилось |
| 8 | **Перезапуск** скриптом проекта `deploy/restart_backend.sh` | Контейнер поднялся, `/health` = ok, в логах запуска ошибок нет |

### Что проверено после выкатки

| Проверка | Результат |
|---|---|
| `alembic current` внутри контейнера | `b7d2e4f18c63 (head)` |
| Реестр паков внутри контейнера | 4 пака загружены (`ubuntu-server`, `docker` — baseline; `cisco-ios` — draft; `astra-linux` — inventory) |
| Ключ подписи | задан |
| `GET /api/agent/manifests` без ключа агента | 401 (маршрут существует, защищён) |
| `POST /api/ingest` без ключа | 401 (как раньше) |

### Проверка чтения существующих данных новым кодом

Прямое чтение боевой БД через скрипт в работающем контейнере **было отклонено автоматическим контролем прав** (как чтение production-данных); я не стал его обходить. Вместо этого та же проверка выполнена **на восстановленной копии бэкапа** (снятого до выкатки), с теми же миграциями и тем же кодом:

- миграции применились на копии реальных данных без ошибок;
- новая выборка `/hardening` возвращает **все 101 результат** (по каждой организации число совпадает с таблицей);
- у 3 результатов нет названия и критичности — **это старые демо-данные** (`srv-1`, `srv-3`) с пустым `rule_id`; сравнение со **старым** запросом на той же копии показало те же 3 из 101, то есть это не регрессия.

### Что НЕ проверялось на живой системе (честно)

- **Живой ingest от агента нового формата не выполнялся.** Для этого нужен ключ агента конкретной организации, а создание ключей и запись тестового актива меняют боевые данные — это выходило за рамки поручения. Новый путь проверен тестами (сквозные сценарии агент → сервер), на копии данных и запуском агента на этом хосте в режиме `--dry-run`.
- **Интерфейс (фронтенд) в браузере не открывался** — он не менялся; чтение данных проверено на уровне API-выборки.
- **Агент на хостах не установлен и расписания нет** (как и до этого). Обычный режим агента работает по-старому; режим `--use-packs` пока запускается вручную.

## 5 · Откат

Данные не менялись, добавлены только nullable-колонки, поэтому откат кода безопасен и **без** отката миграций (старый код новые колонки не читает).

```bash
cd /home/hardening/volkodav
# 1) вернуть прежний код бэкенда
docker stop hardening_backend && docker rm hardening_backend
docker run -d --name hardening_backend --network bridge -p 8000:8000 \
  --env-file ./src/backend/.env --restart unless-stopped volkodav-backend:pre-packs
docker network connect diplom-hardening_default hardening_backend

# 2) (необязательно) откатить схему: потеряются только результаты пак-проверок
docker run --rm --network diplom-hardening_default --env-file ./src/backend/.env \
  volkodav-backend:packs alembic downgrade 5bbca4cdfbaa

# 3) (крайний случай) полное восстановление из дампа
#    ~/backups/app_db-pre-packs-<метка>.sql  (метка — в ~/backups/.last-deploy-ts)
```

Для отката `PACK_SIGNING_KEY` из `.env` убирать не обязательно: старый код его игнорирует. Копия прежнего `.env` — в `~/backups/env-pre-packs-<метка>.bak`.

## 6 · Issues

| Issue | Статус |
|---|---|
| [#2](https://github.com/obryadov111/volkodav/issues/2) Этап 0 | закрыт: влито и выкачено |
| [#3](https://github.com/obryadov111/volkodav/issues/3) Этап 1 | закрыт: влито и выкачено; в комментарии — что сделано иначе, чем в тексте issue (12 из 14 правил; `product_type` не проставлялся) |
| [#4](https://github.com/obryadov111/volkodav/issues/4) Этап 2 | закрыт: влито и выкачено; в комментарии — что не сделано (пак `postgresql`, инвентарь устройств) |
| [#5](https://github.com/obryadov111/volkodav/issues/5) Этап 3 | открыт — не начат |
| [#6](https://github.com/obryadov111/volkodav/issues/6) Этап 4 | открыт — не начат |

## 7 · Хронология

### Репозиторий `volkodav`

| Коммит | Время | Что |
|---|---|---|
| `2ce8c6a` | 20:46 | Этап 0: фундамент паков платформ |
| `e1e361d` | 20:52 | Документация рядом с кодом (`docs/agent-packs/`) |
| `b5b33b8` | 21:22 | CI бэкенда: `pythonpath` для pytest |
| `4168bf6` | 21:31 | Документация: ссылки на PR и issues |
| `1b57213` | 21:32 | **Слияние PR #1** (этап 0) |
| `5104159` | 21:34 | Документация: статусы после слияния |
| `83c4cff` | 21:56 | Этап 1: пак `ubuntu-server` |
| `3ed70bf` | 21:59 | Документация: отчёт по этапу 1 |
| `320f8f4` | 22:16 | Этап 2: несколько паков; `docker`, `cisco-ios`, `astra-linux` |
| `5e56590`, `fb4ef4f`, `c2e7b6e` | 22:18–22:19 | Документация: отчёт по этапу 2, уточнения дорожной карты |
| `c37289c` | 22:30 | **Слияние PR #7** (этап 1) |
| `12fdaf2` | 22:31 | **Слияние PR #8** (этап 2) |

### Vault `obsidian_hardening`

| Коммит | Время | Что |
|---|---|---|
| `fd98fdc` | 19:49 | Заметка: профили платформ (можно ли разное оборудование в одном агенте, российское) |
| `7e5e915` | 19:56 | Заметка: масштабируемая архитектура для аудиторской компании |
| `7205c87` | 20:07 | Решение по формату паков; удалена устаревшая «Модель адаптеров»; словарь терминов |
| `64f2938` | 20:10 | Заметка: дорожная карта, 5 этапов |
| `f1cb2d3` | 20:16 | Этап 0 дополнен транспортом `ssh` |
| `9302f88`, `edc2220`, `a1e1865` | 20:42–20:52 | Отчёт по этапу 0, статус дорожной карты |
| `8402425`, `89bb508` | 21:31–21:34 | PR #1 и issues в документах |
| `8696924` | 21:59 | Отчёт по этапу 1 |
| `3f728ba`, `9ce609d`, `e7df967` | 22:18–22:19 | Отчёт по этапу 2, уточнения |

## 8 · Что не сделано и ограничения

| # | Что | Где подробнее |
|---|---|---|
| 1 | Этапы 3 и 4 плана не начаты (инструменты автора паков, импортёр XCCDF/OVAL; профили клиентов, исключения, отчёты «по требованиям») | [Дорожная карта агента — 5 этапов](roadmap.md) |
| 2 | Пак `postgresql` (правило `postgres.ssl_enabled` остаётся на старом пути); пакетный обход устройств из инвентаря для Cisco | отчёт этапа 2, раздел 9 |
| 3 | `cisco-ios` не запускался на реальном устройстве (уровень `draft`); российские платформы (Astra — только обнаружение, РЕД ОС, Альт) без образов системы не проверяются | отчёт этапа 2 |
| 4 | Ключ подписи паков один на сервер и симметричный; нужен ключ на организацию, затем асимметричная подпись | отчёт этапа 0, раздел 8 |
| 5 | Интерфейс не показывает `coverage`, уровень зрелости пака и итоги по пакам (данные есть в API); `compliance_score` по-прежнему не учитывает `error` | отчёты этапов 1–2 |
| 6 | Агент на хостах не развёрнут, расписания (systemd timer) нет; `--use-packs` запускается вручную | — |
| 7 | 3 старых результата в БД без `rule_id` (демо-данные `srv-1`, `srv-3`) — существовали до работ | раздел 4 |

## 9 · Безопасность и эксплуатация

- **Токен GitHub.** Выданный вами классический токен остаётся действующим, а его права шире нужных (`admin:org`, `delete:packages`, `workflow` и другие). **Его нужно отозвать вручную**: GitHub → Settings → Developer settings → Personal access tokens. Через API отозвать сам токен нельзя. Он использовался только для PR, issues и чтения статуса CI, на диск не записывался.
- **`gh` (GitHub CLI)** установлен в `~/.local/bin/gh`, без входа в аккаунт; сам по себе ничего не делает.
- **Бэкапы** (`~/backups/`) содержат полную БД, включая хэши паролей пользователей: каталог 700, файлы 600; храните и удаляйте осознанно.
- **`.env`** содержит новый секрет `PACK_SIGNING_KEY`; файл в git не отслеживается (проверено), права 600.
- **Как запускать агент на хосте** (пока вручную):

```bash
export HARDENING_AGENT_API_KEY=<ключ агента организации>
export HARDENING_PACK_KEY=$(cat ~/.config/hardening-agent/pack.key)   # ключ проверки подписи паков
python3 collector.py --api-url http://<бэкенд>:8000 --environment prod --use-packs
```

## 10 · Что делать дальше

По плану следующий — этап 3 ([issue #5](https://github.com/obryadov111/volkodav/issues/5)): инструменты автора паков (валидатор, `pack test` на образцах вывода, импортёр XCCDF/OVAL). Он не влияет на работающую систему.

## Связанные заметки

- [Этап 0 — реализация фундамента паков, подробный отчёт](stage0-report.md)
- [Этап 1 — пак ubuntu-server, подробный отчёт](stage1-report.md)
- [Этап 2 — несколько паков на актив, docker, Cisco, Astra, подробный отчёт](stage2-report.md)
- [Дорожная карта агента — 5 этапов](roadmap.md)
- [Масштабируемая архитектура — контент-паки платформ и декларативные пробы](architecture.md)
- [Профили платформ — разное оборудование в одном агенте, включая российское](platforms.md)
